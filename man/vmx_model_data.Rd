% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_versions.R
\name{vmx_model_data}
\alias{vmx_model_data}
\title{Fetch model-ready tidy tables for a data version}
\usage{
vmx_model_data(dv, time_basis = NULL, client = vmx_client())
}
\arguments{
\item{dv}{A data-version id or \code{vmx_data_version}.}

\item{time_basis}{Optional time basis; defaults to the DataVersion's
recommended basis. See \code{\link[=vmx_data_version_table]{vmx_data_version_table()}}.}

\item{client}{A \code{vmx_client}.}
}
\value{
A \code{vmx_model_data} object.
}
\description{
Returns a \code{vmx_model_data} bundle with \verb{$subjects}, \verb{$pk}, \verb{$dosing}, \verb{$pd},
\verb{$labs} and \verb{$covariates} (each a tibble, or \code{NULL} when the DataVersion has
no such prepared table), and \verb{$meta} (units, time bases, the selected
\code{time_basis}, PD-marker manifest, subject count) read from the DataVersion.
Only domains flagged in the DV's \code{table_availability} are fetched, so absent
optional tables don't 404. Every table is projected on the same time basis.
}
